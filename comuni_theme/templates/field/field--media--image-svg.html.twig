{% for item in items %}
  <div{{ item.attributes.addClass('field__item') }}>
    <svg class="icon m-0">
      <image width="100%" height="100%" xlink:href="{{ item.content }}" href="{{ item.content }}"></image>
    </svg>
  </div>
{% endfor %}