<div class="card py-3 px-4 h-100 rounded shadow-sm border border-light">{{ title_suffix }}

  <div class="container px-0">
    <div class="row mx-0 gap-3">
      <div class="col px-0">
        {% include directory ~ '/templates/card/parts/card--parts--category.html.twig' with { 'icon': 'it-chart-line', 'taxonomy': content.field_temi } %}
        {% include directory ~ '/templates/card/parts/card--parts--title.html.twig' with { 'label': label } %}
        {% include directory ~ '/templates/card/parts/card--parts--text.html.twig' with { 'content': content.field_descrizione_breve[0] } %}
      </div>
    </div>
  </div>

  {% include directory ~ '/templates/card/parts/card--parts--read-more.html.twig' with { 'url': url, 'title': label[0]["#context"].value } %}

  {%
    set tmp = [
      drupal_entity('paragraph', node.field_url_dataset[0].target_id)["#paragraph"].field_dataset[0]|file_url,
      drupal_entity('paragraph', node.field_url_dataset[0].target_id)["#paragraph"].field_link[0].uri
    ]
  %}
  {% for item in tmp %}
    {% if item is not empty %}
      {% include directory ~ '/templates/card/parts/card--parts--download.html.twig' with { 'url': item } %}
    {% endif %}
  {% endfor %}

</div>
