{%
  set classes = [
    is_front ? 'homepage',
  ]
%}
{{ page.header }}
<main{{ attributes.addClass(classes) }}>
  {{ page.content|without('title') }}
  {{ page.rating }}
  {{ page.contacts }}
</main>
{{ page.footer }}
